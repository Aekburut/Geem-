<% include ../partials/header %>

้<h1 class="tex-center">ตรางแสดง ชื่อ ราคา สินค้า</h1> 
<div class="row">
    <div class="col-2">
        <a href="/users" class="h1 text-dark">&lt; Back</a>
    </div>
    <div class="col-10">
        <h1></h1> 
    </div>
</div>


<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>price</th>
            <th>Product</th>
        </tr>
    </thead>
    <tbody id="myTable">
    <% ureport.forEach(function(ureport) { %>
        <tr>
            <td scope="row"><%= ureport.name %></td>
            
            <td><%= ureport.price %></td>
            <td><%= ureport.title %></td>
            
        </tr>
        <% }); %>
    </tbody>
</table>
<style>
.table{
    
}
</style>



<% include ../partials/footer %>